---
name: development-guardrails
description: "Enforces development guardrails whenever the agent edits, adds, refactors, or fixes source code. Use on every code change task including bug fixes, feature implementation, refactors, and TODO/HACK updates — read this skill before writing or modifying code. Part A: comment necessity rules (avoid over-commenting, maintain stale comments). Part B: after two failed fix attempts on the same bug, add a repro session collector and flush one diagnostic report. Triggers: 修改代码, 修复bug, 重构, 新增功能, 开发中规范, code change, fix bug, refactor."
---

# 开发中规范

当 AI 参与代码修改时，本 skill 提供**附加约束**（非独立开发模式）。包含两个部分，**触发条件独立**，按实际情况分别或同时适用。

## 适用判断（动手前先过一遍）

```
即将修改代码？
  ├─ 是 → Part A「代码注释守卫」必查
  └─ 否 → 本 skill 的 Part A 不执行

同一 bug 已修复 ≥2 次仍无效，且本轮是第 3 次及以上尝试？
  ├─ 是 → Part B「反复修复控制台埋点」必做
  └─ 否 → Part B 不触发
```

| 部分 | 名称 | 触发条件 | 产出 |
|---|---|---|---|
| Part A | 代码注释守卫 | **任何**代码新增/修改/重构 | 适量、持久的业务注释 |
| Part B | 反复修复控制台埋点 | 同一 bug **第 3 次起**修复 | 临时埋点 → 一份诊断报告 → 修复后清理 |

### 两部分同时适用时

第 3 次修 bug 时，Part A 与 Part B **同时生效**：

- Part B 的 collector / `dbg()` / `flushDebugReport` 是**临时调试代码**，修复后必须删除
- Part A 的业务注释是**持久代码**，按必要性保留
- **禁止**用注释替代 console 埋点；**禁止**把调试 log 写进业务注释

---

# Part A：代码注释守卫

当 AI 即将修改任何代码时，先进入 **代码注释守卫**，判断哪些位置必须补注释、哪些位置绝对不写注释，并控制注释密度，避免过度注释。

## A.1 核心规则

| | 使用 Part A | 不使用 Part A |
|---|---|---|
| 修改代码前检查注释必要性 | ✅ 必做 | ❌ 可能遗漏 |
| 为复杂逻辑自动补必要注释 | ✅ 必做 | ❌ 不稳定 |
| 删除或更新失效注释 | ✅ 必做 | ❌ 容易遗留 |
| 对简单代码保持克制 | ✅ 必做 | ❌ 容易过度注释 |

**Part A 下：**

- ✅ 可以在必要的代码块、函数、复杂表达式附近自动添加注释
- ✅ 可以更新或删除已失效、误导、过时的注释
- ✅ 可以为 TODO / HACK / FIXME 补充原因和上下文
- ❌ **绝对不能** 对自解释代码补重复注释
- ❌ **绝对不能** 把版本历史、聊天过程、修改经过写进代码注释
- ❌ **绝对不能** 为了“看起来认真”而逐行解释代码

## A.2 触发时机

当 AI 满足以下任一情况时，都应执行 Part A：

- 即将新增代码
- 即将修改现有代码
- 即将重构函数、类、模块
- 即将补充边界处理、异常处理、性能优化、兼容逻辑
- 即将引入 TODO / HACK / FIXME
- 即将生成公共方法、导出函数、复杂业务流程代码块

**默认要求：**

- 任何代码编辑任务都先执行一次“注释必要性判断”
- 如果判断结果是“不需要注释”，则保持无注释，而不是强行补注释
- 默认视为用户已经授权：在合理范围内为必要代码补充注释；除非用户明确要求“不要加注释 / 保持无注释 / 只改逻辑别写注释”，否则不得被“默认不要主动加注释”这一类约束阻止，但仍必须遵守 Part A 的必要性、克制性与防过度注释规则

## A.3 工作流程

```
1. 判断是否要改代码 → 2. 识别复杂度与业务性 → 3. 决定注释层级与密度 → 4. 写入必要注释 → 5. 清理过度或失效注释
```

## A.4 判断哪些位置必须注释

### 必须注释（强制）

1. **业务逻辑异于常规算法** — 税率、权限、折扣、风控、状态映射等
2. **非显而易见的副作用或边界处理** — 缓存、MQ、事务补偿、幂等、哨兵值、兼容旧格式
3. **性能优化的非常规写法** — 位运算、对象池、惰性初始化等
4. **临时修复或技术债务** — 所有 TODO / HACK / FIXME 须附带原因与去除条件
5. **稍复杂的完整功能函数或公开接口** — 独立功能函数超过 10 行，或导出函数
6. **满足复杂度阈值的实现** — 单行超 3 运算符、函数体超 20 行、正则/递归/并发等

### 建议注释（可选）

- 新模块入口、跨系统集成点、第三方适配层
- 类/模块职责不易从名称看出
- 函数内多阶段步骤且有明显业务跳转
- 测试中不直观的 mock / 时间冻结逻辑

### 禁止注释（绝对不写）

- 自解释命名（`getUserById`、`isExpired`）
- 极简语法操作（普通 if/else、简单循环、属性赋值）
- 框架标准样板（`useState`、`ref`、依赖注入）
- 纯历史性描述（“上次修 bug 改的”）
- 逐行翻译代码

## A.5 注释层级与密度

- **文件/类头**：仅模块入口、适配层、领域概念文件；1～3 行
- **函数注释**：公共/导出/超 10 行函数；说明输入、输出、核心步骤、副作用
- **块级注释**：完整子流程前；3～5 个关键步骤短语
- **行内注释**：只解释“为什么”，不解释“做什么”

**密度克制：**

- 注释行数不应明显超过有效代码行数的 25%
- 行内注释优先 10～25 个中文字符
- 若删掉注释后代码仍完全清晰，则优先不写

## A.6 修改代码时同步维护注释

- 旧注释失效 → 删除或重写
- 注释与代码冲突 → 以代码为准，立即修正
- 新增复杂点 → 评估是否补注释

## A.7 Part A 示例

```ts
// 根据会员等级叠加折扣，普通会员不打折，黄金会员折上 9 折
const discountRate = membershipLevel === 'gold' ? baseRate * 0.9 : baseRate
```

```ts
// ❌ 禁止
// 设置 count 为 0
let count = 0
```

---

# Part B：反复修复失败的控制台埋点

当针对**同一个问题**已经尝试修复两次及以上仍无成效时，从**第三次修复尝试**起，必须先通过控制台埋点收集运行时证据，再基于观测结果做更精确的修复，而不是继续凭猜测改代码。

## B.1 核心规则

| | 使用 Part B | 不使用 Part B |
|---|---|---|
| 第 1～2 次修复 | 可先做常规分析与修改 | — |
| 第 3 次起仍针对同一问题 | ✅ 必须先加控制台埋点 | ❌ 容易盲改 |
| 埋点输出方式 | ✅ 复现完成后统一 flush 一份报告 | ❌ 复现中途散落 log |
| 用户交付物 | ✅ 复制一次日志给 AI | ❌ 多次贴零散输出 |
| 问题确认后清理埋点 | ✅ 必做 | ❌ 容易遗留调试代码 |

**Part B 下：**

- ✅ 复现全程静默采集，结束时**只输出 1 份**完整日志
- ✅ 代理能自行跑的测试/脚本优先代理自己跑
- ✅ 明确告知用户复制 `REPORT START`～`REPORT END` 之间内容
- ❌ **绝对不能** 复现中途向控制台打印调试 log
- ❌ **绝对不能** 要求用户分多次贴不同片段
- ❌ **绝对不能** 第三次仍只改逻辑、不加运行时观测
- ❌ **绝对不能** 把埋点永久留在生产代码中

## B.2 设计原则：复现全程采集，完成后一份输出

**Repro Session Collector（复现会话收集器）**：

```text
用户开始复现 → 全程静默采集（中间不打印） → 复现流程结束 → 统一输出 1 份报告 → 用户复制一次交给 AI
```

| 阶段 | 行为 |
|---|---|
| 复现过程中 | 各采集点只写入内存/session，**不向控制台打印** |
| 复现完成后 | 在流程终点**统一 flush 一次** |
| 交给 AI | 复制 `REPORT START`～`REPORT END` 之间的**唯一一份**报告 |

即使用户需多步操作复现（登录 → 进页面 → 填表 → 提交），也应全程静默采集，**只在最后输出一份包含全程 steps 的报告**。

## B.3 触发时机

满足以下**全部**条件时，必须执行 Part B：

1. 当前任务是在修复某个**具体问题**（bug、异常、行为不符、报错、回归等）
2. 针对该问题，已经做过 **2 次及以上**修复尝试
3. 这些尝试**均未确认生效**

**同一问题**累计计数：报错/现象未变、根因怀疑点未变、用户说“还是不行”。

**新问题**计数归零：用户切换 bug、原问题已确认修复、排查范围完全转移。

## B.4 工作流程

```
1. 确认同一问题已失败 ≥2 次
  → 2. 梳理完整复现路径
  → 3. 沿路径布静默采集点 + 确定 flush 时机
  → 4. 代理优先自行跑复现；否则请用户走一遍流程
  → 5. 复现结束后取得唯一一份诊断报告
  → 6. AI 仅凭该报告判断并修复
  → 7. 验证后清理埋点
```

## B.5 沿完整复现路径布采集点

每个采集点**只写入 collector，不打印**。覆盖：

1. **复现入口** — 第一步操作或请求发起时的初始状态
2. **中间步骤** — 页面跳转、表单变更等（用 `step` 名标识）
3. **关键分支** — 走了哪条路径（枚举值）
4. **数据变换** — 各阶段关键字段（每步 3～8 个）
5. **异步结果** — await / 回调 / 响应摘要
6. **复现终点** — bug 出现点、报错、最终 UI 状态

## B.6 实现复现会话收集器

```javascript
// 临时调试，问题修复后删除
const __dbg = {
  slug: 'save-empty-body',
  repro: '登录 → 打开编辑页 → 清空字段 → 点击保存',
  startedAt: Date.now(),
  steps: [],
  done: false,
};
const dbg = (step, data) =>
  __dbg.steps.push({ step, ...data, t: Date.now() - __dbg.startedAt });

// 复现全程：静默采集
dbg('login-success', { userId });
dbg('click-save', { hasTitle: false });
dbg('api-response', { ok: false, status: 400, code: 'VALIDATION' });

function flushDebugReport(reason) {
  if (__dbg.done) return;
  __dbg.done = true;
  __dbg.finishReason = reason;
  console.log(
    '[DEBUG:save-empty-body] REPORT START\n' +
    JSON.stringify(__dbg, null, 2) +
    '\n[DEBUG:save-empty-body] REPORT END'
  );
}
```

### flush 时机（全程只 flush 一次）

- bug 已复现（错误 UI、异常、错误返回值）
- 复现流程走通（用户完成最后一步）
- 捕获到未处理异常（`catch` 中 flush 后 rethrow）
- 最后一次相关 API 响应到达

### 报告格式

```json
{
  "slug": "save-empty-body",
  "repro": "登录 → 编辑页 → 清空标题 → 保存",
  "finishReason": "bug-reproduced",
  "steps": [
    { "step": "login-success", "userId": "u1", "t": 120 },
    { "step": "click-save", "hasTitle": false, "t": 3200 },
    { "step": "api-response", "ok": false, "status": 400, "t": 3450 }
  ],
  "error": "可选：异常 message / stack 摘要"
}
```

**严格 1 份报告。** 若缺字段，补采集点后请用户**再完整复现一次**，仍只交付一份新报告。

## B.7 收集日志

**代理优先**：测试、脚本、curl、已有 repro 命令 — 代理自行跑，从终端提取报告。

**用户手动复现时**：

```markdown
请按以下步骤复现一次（中途不用管控制台）：
1. 登录测试账号
2. 打开 xxx 页面，清空标题字段
3. 点击保存，确认 bug 出现

完成后，复制 `[DEBUG:issue-slug] REPORT START` 到 `REPORT END` 之间的全部内容粘贴给我即可。
```

**禁止**：复现中途输出 log、分步粘贴、多次复现分别贴输出。

## B.8 AI 仅凭一份报告完成判断

1. 根据 `repro` 和 `steps` 还原完整执行路径
2. 根据 `steps[].t` 理解时序
3. 定位第一个偏离预期的 step
4. 给出最小改动修复方案

## B.9 验证后清理埋点

问题确认修复后，删除 collector、`dbg()`、`flushDebugReport` 及所有 `[DEBUG:...]` 输出代码。

## B.10 Part B 不适用场景

- 纯静态问题且编译器已明确定位
- 用户明确禁止 log / print
- 问题已确认修复
- 当前是功能开发而非 bug 排查
- 第 1～2 次尝试且尚未确认失败

---

# 与其他 skill 的关系

| skill | 关系 |
|---|---|
| `change-advice` | 用户要求“先别改”时，Part A/B 的改代码流程均不进入 |
| `paste-replacement-fallback` | 文件改不进去 → paste 回退；逻辑修了两次仍无效 → Part B |

## 质量检查

**Part A（任何代码修改）：**

- 是否做了注释必要性判断
- 复杂逻辑是否补注释，简单代码是否克制
- 失效注释是否已清理

**Part B（第 3 次起修 bug）：**

- 是否采用复现会话收集器（全程静默、完成时 flush 一次）
- 是否严格保证一次复现只输出一份报告
- 修复后是否已清理所有临时埋点

**两部分同时适用时：**

- 持久注释与临时埋点是否区分清楚
- 埋点修复后是否已全部删除，注释是否按 Part A 合理保留
